const { Service, Inject } = require("typedi");
const config = require("../config/index");
const { randomBytes } = require("crypto");
import { IUser, IUserInput} = require("../interfaces/IUser");

export default class AuthService {}
