{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "hiring manager",
    "description": "The hiring manager",
    "required": ["user_id"],
    "additionalProperties": true,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the hiring manager."
        },
        "user_id": {
            "$id": "#/properties/user_id",
            "type": "integer",
            "title": "The user_id of the hiring manager."
        },
        "website_url": {
            "$id": "#/properties/website_url",
            "type": "integer",
            "title": "The website_url of the hiring manager."
        },
        "active": {
            "$id": "#/properties/active",
            "type": "boolean",
            "title": "The active of the hiring manager."
        },
        "latitude": {
            "$id": "#/properties/latitude",
            "type": "number",
            "title": "The latitude of the hiring manager."
        },
        "longitude": {
            "$id": "#/properties/longitude",
            "type": "number",
            "title": "The longitude of the hiring manager."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date when the ticket was created."
        },
        "updated_at": {
            "$id": "#/properties/updated_at",
            "type": "string",
            "title": "The date when the ticket was updated."
        },
        "deleted_at": {
            "$id": "#/properties/deleted_at",
            "type": "string",
            "title": "The date when the hiring manager was deleted."
        }
    }
}
