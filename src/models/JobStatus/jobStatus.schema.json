{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "job_status",
    "description": "The job_status",
    "required": ["job_status_name"],
    "additionalProperties": false,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the job_status."
        },
        "job_status_name": {
            "$id": "#/properties/job_status_name",
            "type": "string",
            "title": "The job_status_name of the job_status."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date when the ticket was created."
        },
        "updated_at": {
            "$id": "#/properties/updated_at",
            "type": "string",
            "title": "The date when the ticket was updated."
        },
        "deleted_at": {
            "$id": "#/properties/deleted_at",
            "type": "string",
            "title": "The date when the job_status was deleted."
        }
    }
}
