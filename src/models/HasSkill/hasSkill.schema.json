{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "has_Skill",
    "description": "The has_Skill",
    "required": ["freelancer_id", "skill_id"],
    "additionalProperties": false,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the has_Skill."
        },
        "freelancer_id": {
            "$freelancer_id": "#/properties/freelancer_id",
            "type": "integer",
            "title": "The database ID of the freelancer."
        },
        "skill_id": {
            "$skill_id": "#/properties/skill_id",
            "type": "integer",
            "title": "The database ID of the skill."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date when the ticket was created."
        },
        "updated_at": {
            "$id": "#/properties/updated_at",
            "type": "string",
            "title": "The date when the ticket was updated."
        },
        "deleted_at": {
            "$id": "#/properties/deleted_at",
            "type": "string",
            "title": "The date when the has_Skill was deleted."
        }
    }
}
